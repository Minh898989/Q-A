version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-app
    image: web-hk2/backend-app
    ports:
      - "3009:3009"
    env_file:
      - .env
    command: npm run start
    networks:
      - backend-network

networks:
  backend-network:
    driver: bridge